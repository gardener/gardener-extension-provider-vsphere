cfgs:
  default:
    branches: ['main', 'e2e', 'test-runner']
  gardener-extension-provider-vsphere:
    index: 1
    branches: ['release-.*']
    inherit:
      default:
        jobs:
          release:
            traits:
              release:
                nextversion: 'bump_patch'

